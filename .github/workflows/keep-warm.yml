name: Keep API Warm

on:
  schedule:
    - cron: "*/15 * * * *"  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -fsS https://salmon-grass-041707b1e.2.azurestaticapps.net/health > /dev/null
